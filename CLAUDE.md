# プロジェクト概要

複数WordPressサイトのセキュリティ状態(CloudSecure WP Security等)と死活状態を
一元表示するダッシュボードシステムの、全体設計・アーキテクチャ決定事項を管理する
リポジトリ。実装コードは含まない。

- `spec/architecture.md` — 通信方向・認証方式・相互接続性の位置づけ(プロトコル仕様)
- `spec/data-schema.md` — クライアント→ホスト間のレポートデータスキーマ(プロトコル仕様)
- `docs/` — 検討中の設計課題(現在は空。Issueでの検討過程の資料を置く)

## 関連リポジトリ

- kagemiru-host — ダッシュボード(ホスト)側プラグイン。REST受信・死活監視Pull・
  DB保存を担当。
- kagemiru-client — 監視対象サイト側プラグイン。セキュリティプラグイン検出・
  アダプタ変換・Push送信を担当。

各リポジトリのTODOや実装詳細は、そのリポジトリ自身のCLAUDE.mdを参照。ここでは
両リポジトリを横断する設計原則のみを扱う。

## ドキュメント運用ルール

- **SSOT**: 確定した仕様は `spec/`、検討中の仕様は `docs/` に置く。
  各プラグインのCLAUDE.mdが参照するのは基本的に`spec/`。ただし検討中の
  仕様であってもCLAUDE.mdからの参照が必要とAIエージェントが判断した場合は
  `docs/`への参照も例外的に許容する。
- **昇格プロセス**: 検討・変更のプロセスはIssueで管理する。検討の過程で
  ドキュメントとして残すべきものは`docs/`に置く。検討結果がまとまった、
  または変更が完了したタイミングでPRを作成し、レビュー承認をもって
  `spec/`へ正式に反映(昇格)したものとする。
- **spec配置の判断基準**: 「他の互換実装(ホスト/クライアント)が従うべき
  内容か」で判定する。
  - 従うべき内容(通信方向・認証方式・レポートJSONスキーマ等) →
    このリポジトリ(Kagemiru)の`spec/`
  - 各プラグイン固有の実装選択(DBスキーマ、暗号化の実装方法、管理画面仕様、
    アダプタパターンの内部設計等) → kagemiru-host/kagemiru-clientそれぞれの
    `spec/`
- **readme.txt**: WordPress.orgディレクトリ提出用の`readme.txt`は成果物
  (提出物)であり、それ自体をSSOTとは扱わない。内容は都度`spec/`や各
  リポジトリの情報から書き起こす。
- **相互接続性**: kagemiru-host / kagemiru-clientは同一の構想の下で開発する
  独立したプロダクト。相互接続性はプロトコル準拠によって担保し、
  「Kagemiru」として保証するのはこの2製品の組み合わせのみ。他の互換実装
  との組み合わせを妨げない。

## Issue運用ルール

- **振り分け基準**: spec配置の判断基準と同様、「host/client双方が従うべき
  内容か」で判定する。
  - プロトコル仕様(通信方向・認証方式・レポートJSONスキーマ等)の新設/変更/
    議論、またはどちらの実装の問題か未確定な設計課題 → このリポジトリ
    (Kagemiru)
  - ホスト固有の実装(REST受信・DB保存・管理画面・死活監視Pull・host側crypto
    実装等)のバグ/機能要望 → kagemiru-host
  - クライアント固有の実装(セキュリティプラグイン検出・アダプタ・Push送信・
    client側crypto実装等)のバグ/機能要望(新規アダプタ追加を含む) →
    kagemiru-client
- **host/client双方に影響する変更(3本立て)**: `spec/data-schema.md` の変更
  のように、host/client双方の実装に影響する変更は以下の3本で管理する。
  1. `kagemiru-host` / `kagemiru-client` それぞれに実装Issueを立てる
  2. `Kagemiru` に全体要件Issueを立て、本文にチェックリスト形式で両Issueを
     参照する(`- [ ] J-KEI/kagemiru-host#N` の形式。参照先がクローズされると
     GitHubの Tracked issues 機能により自動でチェックが付く)
  3. host/client側の各Issueには、コメントで対応するKagemiru側の全体要件
     Issueへの参照(例:「関連: J-KEI/Kagemiru#N」)を追記し、双方向にリンク
     させる
- **クローズ時の連携(手動運用)**: 上記3本立てで連携しているIssueについて、
  host/client側のIssueをクローズする際は、そのIssueが参照している
  Kagemiru側の全体要件Issueに対して、クローズした旨を状況コメントとして
  投稿すること(例:「kagemiru-host#N をクローズしました」)。GitHub Actions
  等による自動化は行わず、Issueをクローズする作業を行うAIエージェント/
  開発者が都度手動でコメントする運用とする。
  全体要件Issue自体のクローズは、host/client双方のIssueの完了を確認した上で
  人間が判断する(自動クローズはしない)。

## 開発時の注意(両リポジトリ共通)

- クライアント側からホストへの通信は必ずPush型(`wp_remote_post`)を維持する。
  セキュリティプラグインがREST APIを無効化するケースがあるため。
- 死活監視のみホストからのPull型。
- `spec/data-schema.md` のJSONスキーマを変更する場合は、host/client両リポジトリの
  実装に影響するため、両方の変更を同時に検討すること。
