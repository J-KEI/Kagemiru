# プロジェクト概要

複数WordPressサイトのセキュリティ状態(CloudSecure WP Security等)と死活状態を
一元表示するダッシュボードシステムの、全体設計・アーキテクチャ決定事項を管理する
リポジトリ。実装コードは含まない。

- `docs/architecture.md` — 通信方向・認証方式・アダプタパターン等の設計決定事項
- `docs/data-schema.md` — クライアント→ホスト間のレポートデータスキーマ

## 関連リポジトリ

- kagemiru-host — ダッシュボード(ホスト)側プラグイン。REST受信・死活監視Pull・
  DB保存を担当。
- kagemiru-client — 監視対象サイト側プラグイン。セキュリティプラグイン検出・
  アダプタ変換・Push送信を担当。

各リポジトリのTODOや実装詳細は、そのリポジトリ自身のCLAUDE.mdを参照。ここでは
両リポジトリを横断する設計原則のみを扱う。

## 開発時の注意(両リポジトリ共通)

- クライアント側からホストへの通信は必ずPush型(`wp_remote_post`)を維持する。
  セキュリティプラグインがREST APIを無効化するケースがあるため。
- 死活監視のみホストからのPull型。
- `docs/data-schema.md` のJSONスキーマを変更する場合は、host/client両リポジトリの
  実装に影響するため、両方の変更を同時に検討すること。
